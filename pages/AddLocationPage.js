import React from "react";
import { View, Text, Stylesheet } from "react-native";

export default function AddLocationsPage() {
  return (
    <View>
      <Text>Add a Locaction on this page</Text>
    </View>
  );
}

const styles = Stylesheet.create({});
