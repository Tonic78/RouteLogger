import React from "react";
import { View, Text, Stylesheet } from "react-native";

export default function LocationDetailPage() {
  return (
    <View>
      <Text>Details of a Locaction on this page</Text>
    </View>
  );
}

const styles = Stylesheet.create({});
